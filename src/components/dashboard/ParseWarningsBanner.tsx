"use client";

// ParseWarningsBanner is no longer needed in the direct-payload architecture.
// Kept as a no-op export so existing imports compile without errors.
export function ParseWarningsBanner(_props: { meta?: unknown }) {
  return null;
}
